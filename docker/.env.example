# Docker Compose Environment Variables
# =====================================
# SECURITY: Copy this file to .env and set strong passwords!
# Generate passwords: openssl rand -base64 32
#
# NEVER commit .env to version control!

# PostgreSQL
POSTGRES_USER=developer
POSTGRES_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS_MIN
POSTGRES_DB=devdb

# Redis
REDIS_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS_MIN

# MongoDB
MONGO_USER=developer
MONGO_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS_MIN

# MySQL
MYSQL_ROOT_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS_MIN
MYSQL_DATABASE=devdb
MYSQL_USER=developer
MYSQL_PASSWORD=CHANGE_ME_USE_STRONG_PASSWORD_32_CHARS_MIN

# =====================================
# Quick password generation commands:
# =====================================
# Linux/macOS:
#   openssl rand -base64 32
#   
# PowerShell:
#   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
#
# Python:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
